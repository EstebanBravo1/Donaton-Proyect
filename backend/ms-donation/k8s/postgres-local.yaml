apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-donation-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-donation-db
  template:
    metadata:
      labels:
        app: postgres-donation-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: "db_donation"
            - name: POSTGRES_USER
              value: "neondb_owner"
            - name: POSTGRES_PASSWORD
              value: "password123"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-donation-service
spec:
  selector:
    app: postgres-donation-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432